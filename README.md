## Bash tut
> review all bash commands for fun
 
 ```
 > how to install:
 goto https://jmeubank.github.io/tdm-gcc/download/
 install, choose gcc and gfortran

 check if gcc or gfortran installed
 - gcc --version
 - gfortran --version
 ```

 ```
 > how to compile and run fortran files

 - gfortran [filename].f90 -o [filename] && ./[filename]

> how to compile and run a fortran file with a fortran module

- gfortran [moduleName].f90 [filename].f90 -o [filename] && ./[filename]
 ```