program c_cd
    implicit none;
    print *, "- The cd command uses to move in or out a directory";
    print *, "- cd frequently uses with the ls command to navigate across the file tree";
    print *, "- we can use cd .. to move out directory";
end program c_cd